## R CMD check results

0 errors | 0 warnings | 1 note

* This is a new release.

## Test environments

* local macOS install, R 4.4.x
* GitHub Actions (ubuntu-latest, windows-latest, macos-latest)
* R-hub (various platforms)

## Dependencies

This package depends on the following CRAN packages:
- cli
- dplyr
- glue
- httr
- httr2
- jsonlite
- purrr
- stringr
- tibble
- tidyprompt

## Notes

* The package provides an interface to Large Language Models (LLMs) and requires 
  network access for API calls.
* Examples are wrapped in \dontrun{} as they require API keys and network access.

## Reverse dependencies

This is a new package, so there are no reverse dependencies.
